/**Create an array to store companies ->"Bloomberg", "Uber", "Google","IBM"," Netflix"
 * Remove the first company from the array
 * b Remove the Uber and add Ola in its place
 * Add Amazon at the end 
*/
let arr=["Bloomberg", "Uber", "Google","IBM","Netflix"];
// Remove the first company from the array
// arr.shift();
// console.log(arr);

// Remove the Uber and add Ola in its place
// arr.splice(1,1,"ola");
// console.log(arr);

// Add Amazon at the end
arr.push("Amazon");
console.log(arr);